<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="ProductTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <Label FontSize="20" Content="{Binding ProductName}"/>
            <Button Grid.Column="1" Background="Transparent" HorizontalAlignment="Center" VerticalContentAlignment="Center" BorderThickness="0"
                    Command="{Binding RemoveCMD}">
                <TextBlock Text="-" FontSize="40" Margin="0,-18,0,0"/>
            </Button>
        </Grid>
    </DataTemplate>
</ResourceDictionary>